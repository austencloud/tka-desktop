[pytest]
addopts = -v
pythonpath = . modern v2/src v2/tests # Ensuring all relevant paths are present
qt_api = pyqt6

markers =
    unit: Fast unit tests
    integration: Component integration tests
    ui: User interface tests (now with pytest-qt)
    parity: Legacy functionality parity tests
    slow: Tests that take >5 seconds
